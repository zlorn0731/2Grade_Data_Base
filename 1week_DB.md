# 📚 2학년 데이터베이스 (Data Base)
## 01 : DB 기본 개념

### 데이터와 정보
- 데이터(Data)
  - 현실세계에서 관찰하거나 측정하여 수집한 사실이나 값
  - 숫자, 문자열, 텍스트, 이미지, ...
- 정보(Information)
  - 의사결정을 할 수 있게 하는 데이터의 해석이나 데이터 상호간의 관계
  - 데이터를 처리해서 얻어진 결과

### 정보 처리
- information processing
- 데이터에서 정보를 추출하는 과정 또는 방법

### 유용한 정보
```
Q. 성결대에서 가장 가까운 지하철역은?

- A1 : 안양역
- A2 : 명학역

 | 정확성(accuracy)! |
```
```
Q. 성결대 컴퓨터공학과 입학정원은 몇 명인가?

- A1 : 80명 ------- 17년 이전
- A2 : 70명 ------- 25년 이전
- A3 : 54명 ------- 25년 이후

 | 현재성(currency)! |
```
- 어떤 정보가 유용한 것인가?
  - 의사결정 과정에 반영되어 유용한 결과를 가져올 때
  - 정확성과 현재성을 가지고 있어야 함
  - 결국, 데이터가 현재 상황을 정확하게 반영한 것이어야 함

### 데이터베이스와 정보 시스템
- 데이터베이스
  - 데이터를 단순히 모아 두는 게 아니라, 정확성과 현재성을 갖도록 수집/관리해야함
- 정보 시스템
  - 조직 운영에 필요한 데이터를 수집하여 저장해 뒀다가 의사결정이 필요할 때 처리하여 유용한 정보를 만듦
 
### 데이터베이스
- Database : DB
- 한 조직의 여러 응용 시스템이나 사용자들이 [공용]할 수 있도록 [통합], [저장]된 [운영] 데이터의 집합

### 공용 데이터
- Shared data : 공유 데이터
- 조직의 여러 응용 시스템들이 [공동]으로 [소유]하고 [유지]하며 이용하는 데이터
- 여러 사용자가 서로 다른 목적으로 공용
  - 사용 목적이 다른 사용자들을 두루 고려하여 DB를 구성해야 함
- 데이터 양 대형화, 구조 복잡

### 통합 데이터
- Integrated data
- 원칙적으로 데이터 중복(data redundancy)을 허용하지 않음
  - 효율성을 위해 불가피하게 중복을 허용하기도 함
- 데이터베이스는 통제된 중복(controlled redundancy)이 허용된 통합된 데이터

### 저장 데이터
- Stored data
- 컴퓨터가 접근할 수 있는 저장 매체에 저장된 데이터
  - 조직 내부 컴퓨터뿐만 아니라 클라우드 환경에서 저장/관리되는 경우도 많음

### 운영 데이터
- Operational data
- 조직 고유의 기능을 수행하기 위해 반드시 유지해야 될 데이터
- 데이터베이스는 단순한 데이터 집합이 아니라 조직의 존재 목적이나 기능 수행에 반드시 필요한 데이터의 집합
  - 단순 입출력 데이터 X, 임시 데이터 X
 
### 데이터베이스의 특징
- 실시간 접근성(real-time accessibility)
  - 데이터 요구에 대해 실시간 처리 및 응답
- 계속적인 변환(continuous evolution)
  - 현실 세계는 끊임 없이 변하고 있음
  - 현실 세계를 표현하는 데이터베이스 역시 계속적으로 변화해야 함
  - 데이터베이스 상태는 동적임(dynamic)
    - 삽입(insert), 삭제(delete), 수정(갱신 : updata) → 현재의 정확한 데이터 유지
- 동시 공용(concurrent sharing)
  - 여러 사용자가 동시에 원하는 데이터에 접근하여 이용
    - 서로 다른 데이터 동시 사용
    - 같은 데이터를 동시 사용
- 내용에 의한 참조(contents reference)
  - 데이터의 위치나 주소가 아닌 내용(값)에 따라 참조
  - 조건에 맞는 데이터가 서로 다른 위치에 저장되어 있어도 모두 검색
 
### 데이터의 유형 (유형별로 적합한 저장/처리 기술 선택)
- 구조화된 형태에 따른 분류
  - [정형 데이터] (기계어)       ↘
  - 반정형 데이터 (기계어)        → 생성형 AI 때문에 세가지 다 자주 사용
  - 비정형 데이터 (사람어)       ↗

- 데이터의 특성에 따른 분류
  - 수치형 데이터
  - 범주형 데이터
  - 텍스트 데이터
 
#### 형태 : 정형 데이터
- Structured data
- 구조화된 데이터
  - 미리 정해진 구조에 따라 저장된 데이터
- 데이터 구조에 대한 설명과 데이터 내용은 별도로 유지됨
- (예시) : 관계 DB의 테이블, 엑셀 스프레드시트

#### 형태 : 반정형 데이터
- Semi-structured data
- 구조에 따라 저장된 데이터이지만, 데이터 내용 안에 구조에 대한 설명이 함께 존재
- 구조를 파악하는 파싱(parsing) 과정 필요, 보통 파일 형태로 저장
- (예시) : HTML, XML, JSON 문서, 웹 로그

#### 형태 : 비정형 데이터
- Unstructured data
- 정해진 구조가 없이 저장된 데이터
- (예시) : 텍스트, 영상, 이미지, 음성
- 생성형 AI로 인해 비정형 데이터의 중요성 ↑

#### 특성 : 수치형 데이터
- Numerical data
- 크기 비교와 산술적인 연산이 가능한 숫자 값을 가진 데이터 : 양적 데이터(quantitative data)
- 이산형 데이터(discrete data) - 정수
  - 개수를 셀 수 있는 띄엄띄엄 단절된 숫자 값을 가지는 데이터
  - (예시) : 고객 수, 판매량, 합격자 수
- 연속형 데이터(continuous data) - 실수
  - 연속적으로 이어진 숫자 값을 가지는 데이터
  - (예시) : 키, 몸무게, 온도, 점수
 
#### 특성 : 범주형 데이터
- Categorical data
- 범주로 구분할 수 있는 값, 즉 종류를 나타내는 값을 가진 데이터
- 크기 비교와 산술적인 연산이 가능하지 않아 질적 데이터(qualitative data)라고도 함
- 명목형 데이터(nominal data)
  - 순서, 즉 서열이 없는 값을 가지는 데이터
  - (예시) : 성별, 혈액형, MBTI 유형, 거주 지역(도시)
- 순서형 데이터(ordinal data)
  - 순서, 즉 서열이 있는 값을 가지는 데이터
  - (예시) : 학년, 학점 등급, 회원 등급

- 범주형 데이터에 대해 평균을 계산하여 분석하는 것이 적절할까?
  - 수치형은 적절 범주형은 필요X
 
#### 특성 : 텍스트 데이터
- Text data
- 범주형 데이터도 대부분 문자 데이터지만, 범주형 데이터는 정해진 (짧은) 텍스트 중 하나
- 가능한 값이 정해져 있지 않은 텍스트 데이터들도 있음
- (예시) : 뉴스 본문, 문자메시지, 댓글
- 이외에도 음성, 이미지, 영상 등 다양한 분류 가능

#### 정리 문제 1 : 데이터의 유형
- 다음 데이터의 유형을 분류하시오
  - 영화 관람 후기 - 텍스트 비정형
  - 도서 DB의 도서 장르 - 범주 정형
  - 통화 내용 녹음한 음성 파일 - 텍스트 비정형
  - 고객 DB의 가입 고객 수 - 수치 이산 정형
  - 물류 창고 관리를 위해 온도/습도를 시간별로 기록한 센서 데이터 - 수치 연속 정형
 
##### ✍️작성자: 박지안
##### 🗓️ 작업일: 2026-09-04
